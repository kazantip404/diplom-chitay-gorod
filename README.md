 Тестирование сайта "Читай-город"
    Содержит API и UI тесты.

Запуск тестов, но прежде получи токен (см. ниже)
pytest tests/test_api.py -v
pytest tests/test_ui.py -v

                     ⚠️ Важно:
!!!!!!!!!!!!!!ТОКЕН РАБОТАЕТ СУТКИ!!!!!!!!!!!!!
!!!!!!При ошибке 401 нужно обновить токен!!!!!
↓↓↓↓↓↓Без токена API тесты не будут работать (ошибка 401).↓↓↓↓↓↓

                 Шаги получения токена:
1. Зайти на сайт: https://www.chitai-gorod.ru

2. Открыть DevTools: F12 → вкладка "Сеть" (Network)

3. Перейти во вкладку "Приложение" в DevTools

4. Слева выбрать файлы COOKIE 

5. Ввести token (в строку поиска там же в DevTools)

6. Создать файл config/tokens.py

7. Скопировать значение токена Bearer%20eyJ0eXAiOiJ.... 

8. Для работы токена убрать лишнее символы, пример "Bearer eyJ0eXAiOiJKV1QiLCJ...."

9. Токен вставить в файл: config/tokens.py 

При ошибке 401 нужно обновить токен
Просто замените старый токен на новый в config/tokens.py

▶️ Запуск тестов
API тесты (5 тестов):
bash
pytest tests/test_api.py -v
Тестирует:

Поиск "Лев Толстой"

Поиск "книга"

Поиск "детектив"

Популярные запросы

Доступность API

UI тесты (полный сценарий):
bash
pytest tests/test_ui.py -v
        -----Тестирует:----

Поиск книги ↓

Открытие карточки товара ↓

Добавление в корзину ↓

Редактирование кол-во товара (+1, -1) ↓

Очистка корзины.

📁 Структура проекта

Chitay_gorod/
├── api/                         # 🔌 Работа с API
│   ├── __init__.py
│   └── base_client.py           # 📡 Базовый HTTP-клиент
├── config/                      # ⚙️ Настройки проекта
│   ├── __init__.py
│   ├── settings.py              # ⚙️ Основные параметры
│   └── tokens.py                # 🔐 Токены (секретные данные)
├── pages/                       # 🖥️ Page Objects (UI)
│   ├── __init__.py
│   ├── base_page.py             # 🏗️ Базовый класс страниц
│   ├── cart_page.py             # 🛒 Страница корзины
│   ├── product_page.py          # 📦 Страница товара
│   └── search_page.py           # 🔍 Страница поиска
├── tests/                       # 🧪 Тесты
│   ├── __init__.py
│   ├── test_api.py              # 🚀 API-тесты
│   └── test_ui.py               # 🌐 UI-тесты
├── .gitignore                   # 🙈 Игнорируемые файлы
├── conftest.py                  # 🛠️ Фикстуры Pytest
├── Readme.md                    # 📖 Документация
└── requirements.txt             # 📦 Зависимости

 _______Для получения отчёта Allure следующие команды______ 
Шаги:  
1. pytest --alluredir=allure-results формирует тестовый отчёт 

2. allure serve allure-results запуск получившегося отчёта 

В 1-ый раз обязательно так по шагам, после 1-го прогона можно и со 2 шага


                🛠️ Решение проблем
❌ Ошибка 401
Решение: Обновить токен 


